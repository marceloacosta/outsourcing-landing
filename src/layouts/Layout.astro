---
import { SEO } from "astro-seo";
import Footer from "@components/footer.astro";
import Navbar from "@components/navbar/navbar.astro";
import "@fontsource-variable/inter/index.css";
import googleanalytics from "@components/googleanalytics.astro";
import opengraph from "assets/opengraph.jpg";
import ContactForm from "@components/contactform.astro";






export interface Props {
  title: string;
}

const canonicalURL = new URL(Astro.url.pathname, Astro.site).toString();

const resolvedImageWithDomain = {opengraph};

const { title } = Astro.props;

const makeTitle = title
  ? title + " | " + "ZirconTech"
  : "ZirconTech";
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.jpg" />
  ¡

    <!-- <link rel="preload" as="image" href={src} alt="Hero" /> -->
    <SEO
      title={makeTitle}
      description="ZirconTech Agile Development Services"
      canonical={canonicalURL}

      <meta property="og:title" content=Agile Development Services>
      <meta property="og:site_name" content=ZirconTech>
      <meta property="og:url" content=https://agile.zircon.tech/>
      <meta property="og:description" content=Elevate your product development with a partner that understands the need for flexibility, speed, and efficiency. >
      <meta property="og:type" content=website>
      <meta property="og:image" content=https://9247ced2.rocketcdn.me/wp-content/uploads/2024/02/banking-transactions-1.png.webp>
    />
  <googleanalytics />
  </head>
  <body>
    <Navbar />
    <slot />
    <ContactForm />

    <Footer />
    <style is:global>
      /* Improve Page speed */
      /* https://css-tricks.com/almanac/properties/c/content-visibility/ */
      img {
        content-visibility: auto;
      }
    </style>
  </body>
</html>
